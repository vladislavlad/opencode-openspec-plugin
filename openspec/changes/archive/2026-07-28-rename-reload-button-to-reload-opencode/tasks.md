## 1. Rename Reload button label

- [x] 1.1 In `src/components/settings.tsx`, change `label="Reload"` to `label="Reload OpenCode"` on the reload button

## 2. Migration entry

- [x] 2.1 Add a MIGRATIONS entry in `src/lib/migrations.ts` for this release version with `releaseNotes` noting that the Settings reload button label is now consistent with the init-flow banner

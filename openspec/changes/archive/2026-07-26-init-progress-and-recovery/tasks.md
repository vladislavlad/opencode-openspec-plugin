## 1. Обнаружение корня по config.yaml

- [x] 1.1 В `src/lib/openspec.ts` добавить помощник листинга записей с проглатыванием ошибки и переписать обнаружение корня: директория считается корнем, если содержит поддиректории **или** файл `config.yaml`
- [x] 1.2 Проверить, что `readOpenSpec` возвращает summary с пустыми `specs`/`changes`, когда подпапок ещё нет (отсутствующие `${root}/specs` и `${root}/changes` дают пустые списки)
- [x] 1.3 Обновить/добавить юнит-тесты обнаружения корня: только `config.yaml`; только поддиректории; ничего

## 2. Чтение маркера настройки

- [x] 2.1 В `src/lib/updates.ts` добавить `readInitFlag(client)`: парсинг `openspec/config.yaml` (корни `openspec`/`.openspec`), возврат `{ inProgress: boolean, done: InitStage[] }` — `plugin.init.in-progress` и `plugin.init.done` с отбрасыванием неизвестных значений
- [x] 2.2 Подключить `readInitFlag` в `load()` рядом с `readUpdateFlag` (дёшево на каждом опросе, чтобы баннер гас после снятия маркера агентом)

## 3. Промпты: маркер, чекпоинты, снятие

- [x] 3.1 Проверить на живом CLI, что `openspec init` не перезаписывает существующий `config.yaml` и что повторный init поверх готового проекта безопасен
- [x] 3.2 В preflight добавить запись маркера `plugin.init.in-progress: true` **до** установки CLI и откат маркера при выборе «Cancel»
- [x] 3.3 Добавить чекпоинты `plugin.init.done` после каждого этапа: `["tooling"]` → `["tooling","config"]` → `["tooling","config","specs"]`
- [x] 3.4 В финальный блок добавить `openspec validate --specs`, исправление ошибок и снятие блока `init:` только при успешной валидации — во всех ветках завершения
- [x] 3.5 Добавить `INIT_DISMISS_PROMPT`, снимающий блок `init:` и не делающий ничего больше
- [x] 3.6 `OPENSPEC_INIT_ONLY_PROMPT` (резервный путь) тоже снимает маркер, чтобы настройка не зависала помеченной
- [x] 3.7 В `CONFIG_PROMPT` добавить требование сохранять существующий блок `plugin:` без изменений при перезаписи config.yaml

## 4. Статус-строка вместо полноэкранного индикатора

- [x] 4.1 Вывести этап настройки из `plugin.init.done` — первый незачекпоинченный: «Installing OpenSpec» → «Configuring project» → «Deriving specs» → «Validating specs»
- [x] 4.2 Заменить полноэкранный блок «Initializing» компактной строкой статуса с текстом фазы и анимированной точкой; сохранить анимацию 500 мс
- [x] 4.3 Снять гейт `!setupInProgress()` с ветки браузера, оставив его для экрана `NotInitialised`; убедиться, что экран Settings по-прежнему имеет приоритет
- [x] 4.4 Проверить живое наполнение: во время деривации секция Specifications раскрывается по первому спеку и пополняется по опросу

## 5. Восстановление после незавершённой инициализации

- [x] 5.1 Баннер незавершённой настройки при выставленном маркере, зачекпоинченном `tooling` и простое агента: кнопки Resume и Dismiss (`warning`)
- [x] 5.2 Resume и Dismiss заблокированы при `busy()` — промпт не отправляется, тост «Wait until the agent finishes working»
- [x] 5.3 Предупреждение над кнопкой Init, если ход завершился без тулинга или маркер остался незачекпоинченным; Init повторяет настройку с начала
- [x] 5.4 Баннер сообщает этап остановки — первый незачекпоинченный этап из `done`
- [x] 5.5 Разбить промпт инициализации на этапы и собрать билдер `buildInitPrompt(done)`, включающий только незачекпоинченное
- [x] 5.6 Resume вызывает билдер с этапами из `plugin.init.done`; юнит-тесты на все варианты сборки
- [x] 5.7 Dismiss отправляет `INIT_DISMISS_PROMPT` (ход агента), а не скрывает баннер локально
- [x] 5.8 Баннер «Reopen opencode …» подавлен, пока маркер выставлен — Reload и Resume/Dismiss взаимоисключающие
- [x] 5.9 Экран Init показывается, если маркер выставлен, а `tooling` не зачекпоинчен, даже при существующих `.opencode`/`openspec`
- [x] 5.10 Кнопка Resume — цвет `secondary`
- [x] 5.11 Объединить приглашение к перезапуску: один текст «Reload opencode to activate new commands and skills» цветом `warning` и кнопка «Reload OpenCode» цветом `error` при удачной и неудачной эфемерной регистрации; ряд действий скрыт только при неудачной
- [x] 5.12 Привести спеку `prompt-sending` в соответствие с кодом: `quitOpencode` отправляет `exit`, а не `app.exit`

## 6. Релиз и верификация

- [x] 6.1 Добавить запись в `MIGRATIONS` (`src/lib/migrations.ts`) для следующего релиза: `releaseNotes` о видимых спеках во время init, корректном определении инициализации и подсветке незавершённого init; `instructions` пустые
- [x] 6.2 Прогнать `bun run typecheck`, `bun run test`, `bun run build`
- [x] 6.3 Проверить вживую на пустом проекте: после init без деривации панель показывает браузер, а не кнопку Init
- [x] 6.4 Проверить вживую полный init с деривацией: спеки появляются в панели по мере записи, флаг снимается, баннер не остаётся
- [x] 6.5 Проверить прерывание хода посреди настройки: после простоя показывается баннер Resume, повтор доводит настройку до конца, не переспрашивая пройденное
- [x] 6.7 Проверить Dismiss: раздел `init` снимается агентом, баннер гаснет, после этого появляется приглашение перезапустить opencode
- [x] 6.8 Проверить отмену установки CLI: маркер и созданный `openspec/` откатываются
- [x] 6.6 `openspec validate init-progress-and-recovery --strict`
